/*
import {LoginPage} from './loginPage.po';
import {browser, element, by, ElementFinder} from 'protractor';

describe('cli-project App', function () {
  let page: LoginPage;
  //let baseUrl: string = 'https://philipphaw.github.io/Service-Composition/#/';
  let baseUrl: string = 'http://localhost:4200/#/';

  it('should create team', () => {
    page.clickCreateTeamButton();
    page.sendKeysToElement(page.getElementByClass('createTeamInputName'),'Team' + Math.random());
    page.sendKeysToElement(page.getElementByClass('createTeamInputPassword'),'123456');
    page.clickCreateButton();
    var createTeamInputNameElement = page.getElementByClass('createTeamInputName');
    expect(createTeamInputNameElement.isPresent()).toBeFalsy();
  });

});
*/
